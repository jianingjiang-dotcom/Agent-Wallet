import { AppLayout } from '@/components/layout/AppLayout';

export default function TssSigning() {
  return (
    <AppLayout showNav={false} showBack title="TSS 签名" pageBg="bg-page">
      <div className="flex-1" />
    </AppLayout>
  );
}
